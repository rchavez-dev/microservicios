services:
  api:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      MONGO_URL: mongodb://mongo:27017
      MONGO_DB: practica3
    depends_on:
      - mongo

  mongo:
    image: mongo:7
    volumes:
      - datos:/data/db

volumes:
  datos: